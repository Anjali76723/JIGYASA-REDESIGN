/**
 * useContactModalTrigger — auto-trigger disabled.
 * The modal only opens via explicit user action (button clicks).
 * This file is kept so any existing imports continue to resolve.
 */

// No-op: does nothing. Auto-open behaviour has been removed.
export function useContactModalTrigger() {}

// No-op: session storage flag no longer needed.
export function markContactModalDismissed() {}
